#include <stdio.h>
#include <stdlib.h>

// Unfinished :9

int main(int argc, char **argv)
{
    int rowSize, colSize;
    int **fistMatrix, **secondMatrix, **sparseMatrix;

    printf("Row and cols size: ");
    scanf("%d %d", &rowSize, &colSize);
}
